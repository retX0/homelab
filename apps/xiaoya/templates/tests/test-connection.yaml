apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xiaoya.fullname" . }}-test-connection"
  labels:
    {{- include "xiaoya.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xiaoya.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
