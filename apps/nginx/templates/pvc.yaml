apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "standard-rwo"
  resources:
    requests:
      storage: 100M