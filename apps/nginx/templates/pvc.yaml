apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "standard-rwo"
  resources:
    requests:
      storage: 100M